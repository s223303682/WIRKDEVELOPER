@model IEnumerable<WIRKDEVELOPER.Models.Prescription>;
@{

    Layout = "~/Views/Shared/SurgeonLayout.cshtml";
}
@using Microsoft.AspNetCore.Identity
@using WIRKDEVELOPER.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
<div class=" col-10 text-right">
    <a asp-controller="Surgeon" asp-action="CreatePrescription" style="width:100%">CREATE NEW PRESCRIPTION</a>
</div>
<h1><div class=" bg-info bg-opacity-50 text-center text-white rounded-1">PRESCRIPTIONS</div></h1>

@if (Model.Count() > 0)
{
    <table class=" table table-bordered table-striped table-success">
        <thead>
            <tr>
                <th>Name</th>
                <th>Gender</th>
                <th>Email</th>
                <th>Date</th>
                <th>prescriber</th>
                <th>urgent</th>
                <th>status</th>
                <th>Update</th>
                <th>Delete</th>


            </tr>
        </thead>
        <tbody>
            @foreach (var prescription in Model)
            {

                <tr>
                    <td>@prescription.Name</td>
                    <td>@prescription.Gender</td>
                    <td>@prescription.Email</td>
                    <td>@prescription.Date</td>
                    <td>@prescription.Prescriber</td>
                    <td>@prescription.Urgent</td>
                    <td>@prescription.Status</td>
                   
                    <td>
                        <div class="">
                            <a asp-controller="Surgeon" asp-action="updatePrescription" asp-route-ID="@prescription.PrescriptionID" class=" btn btn-blue-300 mx-1">Update</a>
                        </div>
                    </td>
                    <td>
                        <a asp-controller="Surgeon" asp-action="DeletePrescription" asp-route-ID="@prescription.PrescriptionID" class=" btn btn-blue-300 mx-1">Delete</a>
                    </td>

                </tr>



            }

        </tbody>
    </table>
}
else
{
    <p>NO prescription has been done</p>
}
